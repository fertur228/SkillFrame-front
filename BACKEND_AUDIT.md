# SkillFrame Backend Audit (blocked)

> **Important:** Cloning the backend repository `https://github.com/fertur228/SkillFrame` failed inside the constrained execution environment (`CONNECT tunnel failed, response 403`). Without access to the codebase we cannot perform the requested audit. The sections below capture the expected structure and explicitly note the missing data.

## Дерево проекта (бэкенд)

Недоступно. Репозиторий не удалось склонировать из-за сетевых ограничений (403). Как только доступ будет получен, требуется зафиксировать реальное дерево каталогов `src/main/java/**` и вспомогательных конфигураций.

## Таблица эндпоинтов

| Метод | Путь | Роли | Описание | Request DTO | Response DTO |
| --- | --- | --- | --- | --- | --- |
| – | – | – | – | – | – |

> Необходимо заполнить после получения доступа к контроллерам/Swagger. Текущие данные отсутствуют.

## Безопасность (аутентификация/авторизация/фильтры/CORS/CSRF)

Информация недоступна из-за невозможности доступа к исходному коду. Требуется проверить:

- используемый механизм аутентификации (JWT, cookie-сессии и т.д.);
- конфигурацию `SecurityFilterChain`, кастомные фильтры и PasswordEncoder;
- настройки CORS/CSRF и домены, разрешённые для фронтенда.

## Сущности и связи

Сведения о JPA-сущностях, миграциях и ограничениях не получены. После разблокировки доступа необходимо описать ключевые таблицы/отношения и наличие миграций (Flyway/Liquibase).

## Несоответствия OpenAPI ↔ код

Проверка невозможна без доступа к OpenAPI/контроллерам. После получения исходников необходимо сверить спецификацию `/v3/api-docs` с реализацией.

## Вывод

**Статус:** `в планах` — аудит заблокирован сетевыми ограничениями. Как только доступ к репозиторию будет возможен, требуется повторно выполнить полный обзор и обновить этот документ фактическими данными.
